---
# ModelScope API Token (required)
# Get your token from: https://modelscope.cn/
api_key: "your-modelscope-token-here"

# Default model to use for image generation
default_model: "Tongyi-MAI/Z-Image-Turbo"

# Default output directory for generated images
default_output_dir: "./generated-images"

# Polling timeout for async tasks (in seconds)
poll_timeout: 300

# Default image dimensions
default_width: 1024
default_height: 1024

# Default number of images to generate
default_count: 1
---

# ModelScope Image Generation Plugin Configuration

## Getting Your API Token

1. Visit [ModelScope](https://modelscope.cn/)
2. Sign in or create an account
3. Go to your profile settings
4. Copy your API Token
5. Run `/modelscope-config api-key="your-token"` to save it

## Configuration Options

| Option | Type | Default | Description |
|--------|------|---------|-------------|
| `api_key` | string | - | Your ModelScope API Token (required) |
| `default_model` | string | Tongyi-MAI/Z-Image-Turbo | Default model for image generation |
| `default_output_dir` | string | ./generated-images/ | Where to save generated images |
| `poll_timeout` | int | 300 | Max wait time for async tasks (seconds) |
| `default_width` | int | 1024 | Default image width |
| `default_height` | int | 1024 | Default image height |
| `default_count` | int | 1 | Default number of images to generate |

## Available Models

Common ModelScope image generation models:
- `Tongyi-MAI/Z-Image-Turbo` - Fast, high-quality images (default)
- Check [ModelScope Hub](https://modelscope.cn/) for more models

## Notes

- This file is auto-generated by `/modelscope-config` command
- Never commit this file to version control (contains sensitive credentials)
- The `.local.md` extension ensures it's ignored by git
